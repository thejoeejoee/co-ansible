- name: Restart mediamtx
  become: true
  ansible.builtin.systemd:
    name: csos-mediamtx.service
    state: restarted

- name: Reload sysctl
  become: true
  ansible.builtin.command:
    cmd: sysctl --system
  changed_when: false
